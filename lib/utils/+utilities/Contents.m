% +UTILITIES
%
% Files
%   A2PigmentTemplateFactory - Returns A2 template function for a provided lambdaMax(s).
%   AbsMax                   - Get the absolute max, corrected by the original sign
%   addTooltip               - Add a tooltip to ui elements.
%   bootstrapCI              - 
%   camelizer                - 
%   cast                     - Cast a character value to a specified class
%   cellFromTable            - 
%   centerFigPos             - 
%   collapseUnique           - Collapse repeated cell entries as determined by columnAnchor.
%   createIrisFigure         - Creates a figure with iris theming
%   determineDepth           - 
%   determineGroups          - Create a grouping vector from cell array input.
%   domain                   - min,max array for each input argument provided
%   drawArrow                - 
%   fastKeepField            - 
%   fastrmField              - Removes supplied fieldnames from struct and returns struct of same size.
%   findParamCell            - FINDPARAMCELL
%   flattenStructs           - FLATTENSTRUCT Flattens a struct array.
%   getColorShades           - GETSHADES Get a color array nshades x (rgb) x ncolors
%   getNearestDataPoint      - locate the nearest x value first and use that position to locate the y value
%   getNearestRange          - GETNEARESTPOINTS Return N points from Values that are closest to a target.
%   getNumWorkers            - local function to handle parpool generation
%   hist2dots                - Create vectors for plotting points uniformly distributed in a historgram.
%   isWithinRange            - Validates if a value is within a given range [inclusive by default]
%   killCommand              - KILLCOMMAND
%   listfields               - listfield listfields(Struct,...) produces a table of field names recurs.
%   notifyUser               - Display a message, returns uifigure handle
%   predictLocalLinear       - determine radius step size for finding local range
%   r_isFielda               - Recursively search a Struct
%   ReadToSession            - Read a supported data file into an Iris Session file.
%   recurseStruct            - Turn structs into N by 2 cells.
%   rep                      - Repeat an element or vector N times with individual elements repeated
%   reshaper                 - reshaper Reshapes input vector into winlen rows with overlap in common. If data
%   rmPkg                    - 
%   scriptRead               - scriptRead  Prepares script files for use in MATLAB uifigure tweaks
%   search_recurse           - finds a full path to a given filename.
%   struct2param             - Convert a struct to name,value pairs.
%   subtractBaseline         - Calculate a constant (or fit) value to subtract from each
%   uniqueContents           - Validate cell contents for multiple cells.
%   unknown2CellStr          - 
%   unknownCell2Str          - Convert a cell's contents to a string (char array)
%   ValidStrings             - A modified version of matlab's validstring. VALIDSTRINGS accepts
